/*
package com.example.healthtrackingapp.data.models

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "workouts")
data class Workout(
    @PrimaryKey(autoGenerate = true) val id: Int = 0,
    val userId: Int,
    val date: String,
    val type: String, // Loại hình tập luyện (vd: chạy bộ, yoga)
    val duration: Int, // phút
    val intensity: String, // Nhẹ, Trung bình, Cao
    val result: String? // Kết quả tập luyện (vd: "Cảm thấy tốt", "Mệt mỏi")
)
*/
