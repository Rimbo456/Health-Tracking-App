/*
package com.example.healthtrackingapp.data.models

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "health_journal")
data class HealthJournal(
    @PrimaryKey(autoGenerate = true) val id: Int = 0,
    val userId: Int,
    val date: String,
    val weight: Float?, // kg
    val bloodPressure: String?, // Ví dụ: "120/80"
    val waterIntake: Int?, // ml
    val otherMetrics: String? // Chỉ số khác (vd: đường huyết, cholesterol)
)

*/
