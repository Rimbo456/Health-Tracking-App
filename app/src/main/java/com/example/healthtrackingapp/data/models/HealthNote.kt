/*
package com.example.healthtrackingapp.data.models

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "health_notes")
data class HealthNote(
    @PrimaryKey(autoGenerate = true) val id: Int = 0,
    val userId: Int,
    val date: String,
    val symptoms: String?, // Triệu chứng xuất hiện
    val severity: String?, // Nhẹ, Trung bình, Nặng
    val duration: String?, // Thời gian xuất hiện triệu chứng
    val treatment: String?, // Hướng xử lý
    val medications: String? // Thuốc đã dùng
)
*/
